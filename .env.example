# Copy this file to .env (never commit actual secrets)
# Example variables used by docker-compose and scripts

# Postgres
POSTGRES_DB=deepwikidb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=replace_with_secure_password

# SQL Server
MSSQL_SA_PASSWORD=Replace_with_Secure_SA_pwd!

# pgAdmin (optional)
PGADMIN_EMAIL=pgadmin@local
PGADMIN_PASSWORD=replace_with_pgadmin_pwd

# Optional overrides for ports (test compose uses different defaults)
# POSTGRES_PORT=5432
# SQLSERVER_PORT=1433
