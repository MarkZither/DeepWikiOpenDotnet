{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SessionResponse",
  "description": "Response from POST /api/generation/session containing new session details",
  "type": "object",
  "properties": {
    "session_id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for the created session"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of session creation"
    }
  },
  "required": ["session_id", "created_at"]
}
